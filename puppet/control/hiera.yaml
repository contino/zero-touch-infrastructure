# Provided to support running Puppet Masterless only
---
version: 5
hierarchy:
  - name: nodes
    path: "nodes/%{trusted.certname}.yaml"

  - name: common
    path: "common.yaml"

defaults:
  data_hash: yaml_data
  datadir: hieradata
